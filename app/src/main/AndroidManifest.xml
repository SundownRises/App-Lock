<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_applock"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppLock">

        <!-- Main Activity - disabled by default, accessed via activity-alias -->
        <activity
            android:name=".ui.MainActivity"
            android:enabled="false"
            android:exported="true"
            android:theme="@style/Theme.AppLock" />

        <!-- Default launcher alias - App Lock -->
        <activity-alias
            android:name=".AppLockAlias"
            android:targetActivity=".ui.MainActivity"
            android:icon="@mipmap/ic_applock"
            android:label="@string/app_name_applock"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Stealth mode aliases -->
        <activity-alias
            android:name=".WeatherAlias"
            android:targetActivity=".ui.MainActivity"
            android:icon="@mipmap/ic_weather"
            android:label="@string/app_name_weather"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".CalculatorAlias"
            android:targetActivity=".ui.MainActivity"
            android:icon="@mipmap/ic_calculator"
            android:label="@string/app_name_calculator"
            android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- Lock Activity - the PIN screen -->
        <activity
            android:name=".ui.lock.LockActivity"
            android:launchMode="singleInstance"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:exported="false"
            android:theme="@style/Theme.AppLock.Fullscreen" />

        <!-- Accessibility Service for foreground app detection -->
        <service
            android:name=".service.AppWatchAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Notification Listener Service for notification privacy -->
        <service
            android:name=".service.NotificationPrivacyService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Foreground service to keep the app alive -->
        <service
            android:name=".service.LockGuardService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Boot receiver to re-arm after reboot -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Screen state receiver for screen-off detection -->
        <receiver
            android:name=".service.ScreenStateReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.SCREEN_OFF" />
            </intent-filter>
        </receiver>

        <!-- Device Admin Receiver -->
        <receiver
            android:name=".admin.LockDeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
